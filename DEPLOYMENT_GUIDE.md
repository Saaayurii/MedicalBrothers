# 🚀 Руководство по деплою MedicalBrothers

## Проблема: Обновления не подгружаются после пересборки

### Причины:
1. ❌ CI/CD деплой не настроен (закомментирован в `.github/workflows/ci.yml`)
2. ❌ Изменения в отдельной ветке (`claude/add-swagger-documentation-*`), не в `main`
3. ❌ Vercel деплоит только из production ветки (обычно `main`)

---

## ✅ Решения

### Вариант 1: Ручной деплой через Vercel CLI (Самый быстрый)

```bash
# Установка Vercel CLI
npm i -g vercel

# Авторизация
vercel login

# Production деплой с текущей ветки
vercel --prod

# Или Preview деплой
vercel
```

**Плюсы**: Быстро, работает с любой ветки
**Минусы**: Нужно делать вручную каждый раз

---

### Вариант 2: Создать main ветку и настроить автодеплой (Рекомендуется)

#### Шаг 1: Создайте main ветку

```bash
# Вариант A: Создать main из текущей ветки
git checkout -b main
git push -u origin main

# Вариант B: Слить в существующую ветку
git checkout master  # если есть master
git merge claude/add-swagger-documentation-01JDpaUZes22UZK3RGsbMuJG
git push origin master
```

#### Шаг 2: Настройте Vercel

1. Откройте https://vercel.com/dashboard
2. Выберите проект **MedicalBrothers**
3. Settings → Git → **Production Branch** = `main` (или `master`)
4. Settings → Git → Enable **Automatic Deployments**

#### Шаг 3: Настройте GitHub Secrets для CI/CD

1. Получите токены Vercel:
```bash
# Получить Vercel Token
vercel login
vercel whoami

# Получить Project ID и Org ID
cd /path/to/MedicalBrothers
vercel link
cat .vercel/project.json
```

2. Добавьте секреты в GitHub:
   - GitHub → Settings → Secrets and variables → Actions → New repository secret

   Добавьте:
   - `VERCEL_TOKEN` - Ваш Vercel token (из `vercel login`)
   - `VERCEL_ORG_ID` - Из `.vercel/project.json`
   - `VERCEL_PROJECT_ID` - Из `.vercel/project.json`

#### Шаг 4: Проверьте workflow

Новый файл `.github/workflows/vercel-deploy.yml` уже создан и будет автоматически деплоить при push в `main`.

---

### Вариант 3: Docker Compose (Для локального развертывания)

Если вы пересобирали контейнеры локально:

```bash
# Остановить и удалить все
docker-compose down -v

# Пересборка без кэша
docker-compose build --no-cache

# Запуск
docker-compose up -d

# Проверка логов
docker-compose logs -f app

# Проверка health
docker-compose ps
curl http://localhost:3000/api/health
```

**Важно**: Docker деплой **НЕ связан** с Vercel. Это два разных окружения:
- **Docker**: Локальное развертывание на вашем сервере
- **Vercel**: Облачный хостинг (production)

---

## 🔍 Проверка деплоя

### Для Vercel:

```bash
# Проверить текущие деплои
vercel ls

# Логи последнего деплоя
vercel logs

# Открыть production URL
vercel open
```

### Для Docker:

```bash
# Проверить статус контейнеров
docker-compose ps

# Посмотреть логи
docker-compose logs -f app

# Проверить Swagger
curl http://localhost:3000/api/docs
curl http://localhost:3000/api/swagger
```

---

## 📊 Swagger документация доступна по адресам:

- **Local**: http://localhost:3000/api/docs
- **Production**: https://medicalbrothers.vercel.app/api/docs
- **OpenAPI JSON**: `/api/swagger`

---

## 🐛 Отладка проблем

### Проблема: Vercel деплоит старую версию

```bash
# Проверить, какой коммит задеплоен
vercel ls --scope=your-team

# Принудительный редеплой
vercel --prod --force
```

### Проблема: Docker контейнеры не обновляются

```bash
# Полная очистка
docker-compose down -v --remove-orphans
docker system prune -af
docker volume prune -f

# Пересборка
docker-compose build --no-cache --pull
docker-compose up -d
```

### Проблема: CI/CD не запускается

1. Проверьте Actions в GitHub: `https://github.com/Saaayurii/MedicalBrothers/actions`
2. Убедитесь, что секреты добавлены
3. Проверьте логи workflow

---

## 📝 Рекомендации

1. **Используйте main ветку** для production кода
2. **Настройте автоматический деплой** через GitHub Actions
3. **Тестируйте локально** с Docker перед деплоем
4. **Используйте preview деплои** для feature веток в Vercel

---

## 🎯 Быстрый чеклист

- [ ] Создал `main` ветку или слил изменения
- [ ] Настроил Vercel на автодеплой из `main`
- [ ] Добавил секреты в GitHub (VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID)
- [ ] Запушил изменения в `main`
- [ ] Проверил деплой: `https://medicalbrothers.vercel.app/api/docs`
- [ ] Swagger UI загружается и показывает новую документацию

---

## 💡 Совет

Для немедленного решения используйте **Вариант 1** (ручной деплой), а затем настройте **Вариант 2** для автоматизации будущих деплоев.

```bash
# Быстрое решение СЕЙЧАС:
vercel --prod

# Долгосрочное решение:
git checkout -b main
git push -u origin main
# + настройте GitHub Secrets
```
