# 🌿 Стратегия работы с ветками

## Текущая ситуация

### Активная ветка (для работы):
```
claude/add-swagger-documentation-01JDpaUZes22UZK3RGsbMuJG
```

**Содержит:**
- ✅ Полная Swagger документация (31+ endpoints)
- ✅ 16 категорий API
- ✅ 10+ схем данных
- ✅ API_DOCUMENTATION.md
- ✅ DEPLOYMENT_GUIDE.md
- ✅ LOCAL_STARTUP_GUIDE.md
- ✅ Vercel deploy workflow
- ✅ Ollama интеграция
- ✅ WebRTC UI
- ✅ Все предыдущие фичи

---

## Рекомендуемая стратегия

### Вариант 1: Создать main ветку (Правильно)

```bash
# Из текущей ветки создать main
git checkout -b main

# Запушить main
git push -u origin main

# Настроить Vercel на деплой из main
# Settings → Git → Production Branch = main
```

**Плюсы:**
- ✅ Стандартная практика (main/master для production)
- ✅ Автоматический деплой при push в main
- ✅ Удобная работа с PR
- ✅ CI/CD работает из коробки

---

### Вариант 2: Ручной деплой с feature ветки (Быстро)

```bash
# Остаться на текущей ветке
# claude/add-swagger-documentation-01JDpaUZes22UZK3RGsbMuJG

# Ручной деплой
vercel --prod
```

**Плюсы:**
- ✅ Быстро (5 минут)
- ✅ Не нужно создавать main
- ✅ Сразу увидите результат

**Минусы:**
- ❌ Нужно делать вручную каждый раз
- ❌ Нет автоматического CI/CD

---

## Пошаговый план (Рекомендуется)

### Шаг 1: Продолжить работу в текущей ветке
```bash
# Проверить текущую ветку
git branch --show-current
# claude/add-swagger-documentation-01JDpaUZes22UZK3RGsbMuJG

# Все изменения делать здесь
git add .
git commit -m "ваше сообщение"
git push
```

### Шаг 2: Создать main когда готово
```bash
# Когда всё протестировано и готово:
git checkout -b main
git push -u origin main
```

### Шаг 3: Настроить автодеплой
```bash
# 1. Получить токены Vercel
vercel login
vercel link
cat .vercel/project.json

# 2. Добавить в GitHub Secrets:
# - VERCEL_TOKEN
# - VERCEL_ORG_ID
# - VERCEL_PROJECT_ID

# 3. Готово! Теперь каждый push в main = автодеплой
```

---

## Структура веток

```
main (production)
├── claude/add-swagger-documentation-* (feature)
└── claude/medical-voice-assistant-* (старая feature)
```

### Рекомендуемая структура:
```
main (production) ← автодеплой
├── develop (staging)
└── feature/* (разработка)
```

---

## Текущий статус

| Ветка | Статус | Назначение |
|-------|--------|------------|
| `claude/add-swagger-documentation-*` | ✅ Активная | Swagger docs + новые фичи |
| `claude/medical-voice-assistant-*` | 🔄 Старая | Предыдущая работа |
| `main` | ❌ Нет | Нужно создать |

---

## Команды для работы

### Посмотреть все ветки
```bash
git branch -a
```

### Переключиться на ветку
```bash
git checkout имя-ветки
```

### Создать новую ветку
```bash
git checkout -b новая-ветка
```

### Слить ветки
```bash
git checkout main
git merge feature-branch
```

### Удалить ветку
```bash
# Локально
git branch -d имя-ветки

# На remote
git push origin --delete имя-ветки
```

---

## Быстрые команды

### Проверить на какой ветке вы сейчас
```bash
git branch --show-current
```

### Посмотреть последние коммиты
```bash
git log --oneline -10
```

### Посмотреть разницу между ветками
```bash
git log branch1..branch2
```

### Синхронизировать с remote
```bash
git fetch origin
git pull origin текущая-ветка
```

---

## FAQ

### Q: Какую ветку использовать для разработки?
**A:** `claude/add-swagger-documentation-01JDpaUZes22UZK3RGsbMuJG` (текущая)

### Q: Какую ветку использовать для production?
**A:** Создайте `main` из текущей ветки

### Q: Можно ли задеплоить без main?
**A:** Да, через `vercel --prod` с любой ветки

### Q: Что делать со старой веткой?
**A:** Можно удалить после слияния в main

### Q: Как переключиться обратно?
```bash
git checkout claude/add-swagger-documentation-01JDpaUZes22UZK3RGsbMuJG
```

---

## Рекомендации

1. ✅ **Используйте текущую ветку** для разработки
2. ✅ **Создайте main** когда всё готово для production
3. ✅ **Настройте автодеплой** через GitHub Actions
4. ✅ **Делайте коммиты часто** с понятными сообщениями
5. ✅ **Пушьте регулярно** чтобы не потерять изменения

---

## Полезные ссылки

- **GitHub**: https://github.com/Saaayurii/MedicalBrothers
- **Vercel Dashboard**: https://vercel.com/dashboard
- **Текущая ветка на GitHub**: https://github.com/Saaayurii/MedicalBrothers/tree/claude/add-swagger-documentation-01JDpaUZes22UZK3RGsbMuJG
