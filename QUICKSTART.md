# 🚀 Быстрый старт - Medical Voice Assistant

## 📋 Предварительные требования

- Node.js 22+
- Docker и Docker Compose
- Git

## ⚡ Запуск за 3 минуты

### 1. Клонирование и установка

```bash
git clone https://github.com/Saaayurii/MedicalBrothers.git
cd MedicalBrothers
npm install
```

### 2. Настройка окружения

```bash
# Создайте .env файл
cp .env.example .env

# Файл .env уже содержит правильные настройки по умолчанию:
# DATABASE_URL=postgresql://medical_user:medical_password@localhost:5432/medical_clinic
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=qwen2.5:latest
```

### 3. Запуск базы данных

```bash
# Запустите PostgreSQL в Docker
docker-compose up -d postgres

# Подождите 5 секунд пока БД запустится
sleep 5

# Примените схему Prisma
npm run db:push

# Заполните тестовыми данными
npm run prisma:seed
```

**Что создалось в БД:**
- ✅ 8 врачей (кардиологи, терапевты, неврологи и т.д.)
- ✅ Расписание врачей (Пн-Пт, 9:00-17:00)
- ✅ 6 справочных записей о клинике
- ✅ Готовая структура для записей, пациентов, консультаций

### 4. Запуск Ollama (AI модель)

```bash
# Запустите Ollama
docker-compose up -d ollama

# Подождите пока контейнер запустится
sleep 10

# Загрузите модель Qwen 2.5 (это займёт несколько минут)
bash scripts/init-ollama.sh
```

**Примечание:** Загрузка модели Qwen может занять 5-10 минут в зависимости от скорости интернета.

### 5. Запуск приложения

```bash
# Запустите Next.js dev сервер с Turbopack
npm run dev
```

## 🎉 Готово!

Откройте браузер:

- 🌐 **Главная страница:** http://localhost:3000
- 👨‍💼 **Админ-панель:** http://localhost:3000/admin
- 🗄️ **Prisma Studio:** http://localhost:5555 (после `npm run prisma:studio`)

## 🎤 Проверка голосового интерфейса

1. Откройте http://localhost:3000
2. Нажмите на кнопку микрофона 🎤
3. Скажите: **"Хочу записаться к кардиологу"**
4. Система распознает речь и ответит голосом!

**Поддерживаемые команды:**
- "Запись на приём к кардиологу"
- "У меня болит голова"
- "Какой режим работы?"
- "Нужна скорая помощь!"

## 🛠️ Полезные команды

### Разработка
```bash
npm run dev              # Запуск dev сервера
npm run build            # Production build
npm run start            # Production server
```

### Prisma
```bash
npm run prisma:studio    # GUI для БД (localhost:5555)
npm run prisma:seed      # Пересоздать тестовые данные
npm run db:push          # Применить изменения схемы
npm run prisma:generate  # Регенерировать Prisma Client
```

### Docker
```bash
docker-compose up -d              # Запустить все сервисы
docker-compose up -d postgres     # Только БД
docker-compose up -d ollama       # Только AI
docker-compose logs -f            # Просмотр логов
docker-compose down               # Остановить всё
```

## 🐛 Решение проблем

### Проблема: "ECONNREFUSED" при запуске

**Решение:** БД не запущена или ещё не готова
```bash
docker-compose up -d postgres
sleep 5
npm run db:push
```

### Проблема: "Module not found: @prisma/client"

**Решение:** Нужно сгенерировать Prisma Client
```bash
npm run prisma:generate
```

### Проблема: Ollama не отвечает

**Решение:** Модель не загружена
```bash
bash scripts/init-ollama.sh
```

### Проблема: Голос не работает в браузере

**Решение:** Используйте Chrome или Edge (Web Speech API)

## 📊 Просмотр данных

### Вариант 1: Prisma Studio (рекомендуется)
```bash
npm run prisma:studio
# Откройте http://localhost:5555
```

**Преимущества:**
- 🎨 Красивый UI
- ✏️ Редактирование записей
- 🔍 Фильтрация и поиск
- 🚀 Быстро и удобно

### Вариант 2: pgAdmin
```bash
docker-compose up -d pgadmin
# Откройте http://localhost:5050
# Email: admin@medicalbrothers.ru
# Password: admin
```

## 🔥 Горячие клавиши

В Prisma Studio:
- `Ctrl/Cmd + K` - Поиск
- `Ctrl/Cmd + S` - Сохранить изменения
- `Esc` - Закрыть модальное окно

## 📚 Дополнительная информация

- Полная документация: [README.md](README.md)
- Prisma схема: [prisma/schema.prisma](prisma/schema.prisma)
- Seed данные: [prisma/seed.ts](prisma/seed.ts)

## 💡 Совет

Для максимальной производительности используйте:
- Chrome/Edge для голосового управления
- NVIDIA GPU для Ollama (опционально)
- SSD диск для БД

---

**Возникли вопросы?** Создайте [Issue](https://github.com/Saaayurii/MedicalBrothers/issues) 🎯
